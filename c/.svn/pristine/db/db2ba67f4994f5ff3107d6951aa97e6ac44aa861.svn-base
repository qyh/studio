#ifndef __QYH__H_
#define __QYH__H_
#include "qyh_string.h"
#include "String.h"
#include "apue.h"
#include "stack.h"
#include "file.h"

void * Malloc(size_t size);

/**
 * Description: Count out how much '1' does value have
 * Return: number of '1' at the form of bit of the parameter value
 */
int find_one_bits( unsigned value );

/**
 * Description: Count out how much '0' does value have
 * Return: number of '0' at the form of bit of the parameter value
 */
int find_zero_bits( unsigned value );

int println(const char * fmt, ...);

#endif
